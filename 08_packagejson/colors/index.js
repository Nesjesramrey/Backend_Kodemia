//import { rainbow} from 'colors' // debido a la version de colors no se pudo utilizar asi, se usa require

const colors = require('colors/safe');

console.log(colors.rainbow('Hola koders! Ya se la saben'));